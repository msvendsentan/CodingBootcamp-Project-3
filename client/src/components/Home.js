import React from 'react';

const Home = () => (
  <div>
    <h2>Identity Selector!</h2>
    <p>Click the symbols above to select "Customer View", or "Restaurant View"</p>
  </div>
);

export default Home;